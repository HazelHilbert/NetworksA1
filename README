Headers:
    packet_type
        1 --> producer anounces stream to broker
                
        2 --> producer broadcasts frame to broker

        3 --> consumer subscribes all

        4 --> consumer unsubscribes all

        5 --> consumer subscribes to stream

        6 --> consumer unsubscribes to stream

        7 --> broker fowards frame to consumer

Making containers:
    docker create -ti --name broker --cap-add=all -v /Users/hazelhilbert/Documents/GitHub/NetworksA1:/compnets pythonimage /bin/bash
    docker network connect csnet broker
    docker start -i broker  
    python3 broker.py